# Disabled: the review skill gets sidetracked on complex PRs and never posts a comment.
# Re-enable when the code-review@claude-code-plugins skill is more reliable.
#
# name: Claude Code Review
#
# on:
#   pull_request:
#     types: [opened, synchronize, ready_for_review, reopened]
#
# jobs:
#   claude-code-review:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       pull-requests: write
#       issues: read
#       id-token: write
#
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 1
#
#       - name: Run Claude Code Review
#         id: claude-code-review
#         uses: anthropics/claude-code-action@ba7fa4bcf054319261202aef93d71a89112a8d00 # v1.0.64
#         with:
#           claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
#           plugin_marketplaces: 'https://github.com/anthropics/claude-code.git'
#           plugins: 'code-review@claude-code-plugins'
#           prompt: '/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}'
#           include_fix_links: true
#           use_sticky_comment: true
#           show_full_output: true
